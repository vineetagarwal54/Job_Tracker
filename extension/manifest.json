{
  "manifest_version": 3,
  "name": "JobTrack — Apply Helper",
  "version": "0.1.0",
  "description": "Scan job application pages, review detected fields, and autofill selected values from a local JobTrack profile. Never submits applications.",
  "permissions": ["activeTab", "scripting", "storage", "sidePanel", "nativeMessaging"],
  "host_permissions": ["http://*/*", "https://*/*"],
  "action": {
    "default_title": "JobTrack — scan this page"
  },
  "side_panel": {
    "default_path": "sidepanel/index.html"
  },
  "background": {
    "service_worker": "service-worker.js",
    "type": "module"
  }
}
