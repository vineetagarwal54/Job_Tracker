export function AiResumeStatus({ status, onTestApi, testing, testMessage, onRefresh }) {
  const items = [
    { label: "Anthropic API", ready: status?.api?.configured, detail: status?.api?.configured ? "Configured" : "Add ANTHROPIC_API_KEY to the root .env file and restart JobTrack." },
    { label: "Tectonic", ready: status?.tectonic?.available, detail: status?.tectonic?.available ? (status.tectonic.version || "Available") : "Unavailable" },
  ];
  return <div style={{ background: "#0e0e18", border: "1px solid #1a1a2e", borderRadius: "12px", padding: "18px" }}><div style={{ fontFamily: "Syne, sans-serif", fontWeight: 700, marginBottom: "12px" }}>Generation readiness</div>{items.map(item => <div key={item.label} style={{ display: "flex", gap: "10px", padding: "9px 0", borderTop: "1px solid #161625" }}><span style={{ color: item.ready ? "#4ade80" : "#f87171" }}>{item.ready ? "●" : "●"}</span><div><div style={{ fontSize: "13px", color: "#e2e8f0" }}>{item.label}</div><div style={{ fontSize: "12px", color: "#94a3b8", marginTop: "2px" }}>{item.detail}</div></div></div>)}<div style={{ display: "flex", gap: "8px", marginTop: "14px", flexWrap: "wrap" }}><button className="btn" onClick={onTestApi} disabled={!status?.api?.configured || testing} style={button}>{testing ? "Testing..." : "Test API Connection"}</button><button className="btn" onClick={onRefresh} style={button}>Refresh status</button></div>{testMessage && <div style={{ marginTop: "10px", color: testMessage.ok ? "#4ade80" : "#f87171", fontSize: "13px" }}>{testMessage.text}</div>}</div>;
}
const button = { background: "#1a1f3a", color: "#a5b4fc", padding: "8px 12px", borderRadius: "7px" };
