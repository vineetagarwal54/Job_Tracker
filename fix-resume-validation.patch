diff --git a/src/generate/lineBudget.js b/src/generate/lineBudget.js
index 1e64a7c..FIXED 100644
--- a/src/generate/lineBudget.js
+++ b/src/generate/lineBudget.js
@@ -11,15 +11,30 @@ export const RESUME_LINE_BUDGET = Object.freeze({
   projectHeadingLines: 1,
 });

+function actionVerb(text) {
+  const match = String(text || "").trim().match(/^([A-Za-z]+)/);
+  return match ? match[1].toLowerCase() : "";
+}
+
 export function estimateBulletLines(chars) {
   return Math.ceil(chars / RESUME_LINE_BUDGET.charactersPerBulletLine);
 }

-export function budgetSelection(resolvedSelection) {
+export function budgetSelection(resolvedSelection, options = {}) {
+  const { requireUniqueActionVerbs = true } = options;
   const includedEntries = new Map();
   const excluded = [];
+  const usedActionVerbs = new Set();
   let usedLines = 0;

   for (const item of resolvedSelection.rankedBullets) {
+    const verb = actionVerb(item.text);
+    if (requireUniqueActionVerbs && verb && usedActionVerbs.has(verb)) {
+      excluded.push({
+        id: item.bullet.id,
+        entryId: item.entry.id,
+        section: item.section,
+        reason: `duplicate action verb '${verb}'`,
+        requiredLines: 0,
+      });
+      continue;
+    }
+
     const key = `${item.section}:${item.entry.id}`;
     const firstForEntry = !includedEntries.has(key);
     const headingLines = firstForEntry
@@ -39,6 +54,7 @@ export function budgetSelection(resolvedSelection) {
       usedLines += headingLines;
     }
     includedEntries.get(key).bullets.push({ ...item, estimatedLines: bulletLines });
+    if (requireUniqueActionVerbs && verb) usedActionVerbs.add(verb);
     usedLines += bulletLines;
   }

diff --git a/src/utils/resumeGeneration.js b/src/utils/resumeGeneration.js
index d3953c0..FIXED 100644
--- a/src/utils/resumeGeneration.js
+++ b/src/utils/resumeGeneration.js
@@ -13,7 +13,10 @@ export const RESUME_ERROR_MESSAGES = {
 };

 export function messageForResumeError(error) {
+  if (error?.code === "VALIDATION_FAILED" && error?.message) {
+    return error.message;
+  }
   return RESUME_ERROR_MESSAGES[error?.code] || error?.message || "Document generation failed.";
 }

diff --git a/src/generate/verifyPhases3to5.js b/src/generate/verifyPhases3to5.js
index 2e095f2..FIXED 100644
--- a/src/generate/verifyPhases3to5.js
+++ b/src/generate/verifyPhases3to5.js
@@ -1,7 +1,7 @@
 import fs from "node:fs"; import path from "node:path"; import { fileURLToPath } from "node:url"; import { createRequire } from "node:module";
-import { applicationProfileToIdentity, resolveResumeIdentity } from "./profileIdentity.js"; import { extractJobKeywords } from "./keywordExtraction.js"; import { scoreCoverage } from "./coverageScoring.js"; import { validateJobAnalysis } from "./jobAnalysisValidation.js"; import { validateContentBank } from "./validateContentBank.js"; import { validateSelection } from "./validateSelection.js";
+import { applicationProfileToIdentity, resolveResumeIdentity } from "./profileIdentity.js"; import { extractJobKeywords } from "./keywordExtraction.js"; import { scoreCoverage } from "./coverageScoring.js"; import { validateJobAnalysis } from "./jobAnalysisValidation.js"; import { validateContentBank } from "./validateContentBank.js"; import { validateSelection } from "./validateSelection.js"; import { budgetSelection } from "./lineBudget.js";
 const require = createRequire(import.meta.url); const { createKeyStore } = require("../../electron/anthropic/keyStore.cjs"); const { parseJsonText } = require("../../electron/anthropic/validation.cjs"); const { createActiveGenerations } = require("../../electron/anthropic/activeGenerations.cjs");
 const here = path.dirname(fileURLToPath(import.meta.url)); const root = path.resolve(here, "..", ".."); const bank = JSON.parse(fs.readFileSync(path.join(here, "content-bank.json"), "utf8")); const selection = JSON.parse(fs.readFileSync(path.join(here, "sample-ai-selection.json"), "utf8"));
 const assert = (condition, message) => { if (!condition) throw new Error(message); }; const rejects = (fn, label) => { try { fn(); } catch { return; } throw new Error(`Accepted ${label}`); };
@@ -12,6 +12,12 @@ const jd = "Build Node.js and C++ model serving for large language models. Use CI/CD and TensorRT-LLM. React Native is preferred."; const extractionA = extractJobKeywords(jd); const extractionB = extractJobKeywords(jd); assert(JSON.stringify(extractionA) === JSON.stringify(extractionB), "Keyword extraction is unstable"); for (const term of ["node.js", "c++", "ci/cd", "tensorrt-llm", "react native", "large language models"]) assert(extractionA.keywords.some((item) => item.normalized === term), `Missing preserved token ${term}`);
 const coverageA = scoreCoverage(bank, extractionA, { analysis: validAnalysis }); const coverageB = scoreCoverage(bank, extractionB, { analysis: validAnalysis }); assert(JSON.stringify(coverageA) === JSON.stringify(coverageB), "Coverage is unstable");
 rejects(() => validateSelection(bank, { ...selection, experience: [{ entryId: "servbeyond-enterprise-ai-platform-intern", bullets: [{ id: "servbeyond-rag-manual-lookup", rewrittenText: "Built 999 systems." }] }], projects: [] }), "invented number");
+const duplicateVerbBudget = budgetSelection({ rankedBullets: [
+  { section: "projects", entry: { id: "first" }, bullet: { id: "first-built" }, text: "Built the first verified project." },
+  { section: "projects", entry: { id: "second" }, bullet: { id: "second-built" }, text: "Built the second verified project." },
+  { section: "projects", entry: { id: "third" }, bullet: { id: "third-created" }, text: "Created a third verified project." },
+] });
+assert(duplicateVerbBudget.included.flatMap(entry => entry.bullets).map(item => item.bullet.id).join(",") === "first-built,third-created", "Duplicate action verbs were not deterministically removed");
 const tempDir = path.join(root, "resume", "output", ".key-test"); fs.mkdirSync(tempDir, { recursive: true }); const fakeSafeStorage = { isEncryptionAvailable: () => true, encryptString: (value) => Buffer.from(`enc:${value}`), decryptString: (buffer) => buffer.toString().slice(4) }; const keyStore = createKeyStore({ safeStorage: fakeSafeStorage, userDataPath: tempDir }); keyStore.saveKey("sk-ant-fake-development"); assert(keyStore.status().configured && keyStore.status().encrypted && !JSON.stringify(keyStore.status()).includes("sk-ant"), "Key status exposed key"); assert(keyStore.readKey() === "sk-ant-fake-development", "Key read failed"); keyStore.deleteKey(); assert(!keyStore.status().configured, "Key delete failed"); fs.rmSync(tempDir, { recursive: true });
